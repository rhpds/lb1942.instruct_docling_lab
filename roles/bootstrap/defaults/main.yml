---
bootstrap_guid: "{{ guid | default('testguid') }}"
bootstrap_rh_submit_repo: https://github.com/jhurlocker/elyra_docling_rh_summit
bootstrap_num_users: "{{ num_users | default(0) }}"
bootstrap_user_name_base: user